There are four .py files.

get_metadata.py works as the main file for getting file metadata in folder.

check_sentence.py checks whether a meaningful file is human readable, e.g. in sentence.

get_extension.py gets the extensions of files.

get_size.py gets the size of files.